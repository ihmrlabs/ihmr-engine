# packages

Shared pieces the apps are built from. Kept small and separately testable.

`health-state/` · `obligation-model/` · `provenance/` · `evaluation/` · `common/`

If something here is hard to test in isolation, that usually means it is doing too much.
