# datasets

Inputs, with provenance recorded: where each came from, when, who published it, and under what
licence.

Where a dataset cannot be redistributed, we keep a manifest and a retrieval script rather than
the data itself.

**No real personal health data is ever stored here.** Synthetic populations are generated, and
where we validate them against real distributions we do it against published aggregates, never
individual records.
